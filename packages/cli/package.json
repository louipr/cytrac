{
  "name": "@cytrac/cli",
  "version": "0.1.0",
  "description": "Command-line interface for Cytrac code analysis",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "cytrac": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "jest",
    "lint": "eslint src/**/*.ts",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@cytrac/core": "0.1.0",
    "commander": "^11.0.0",
    "chalk": "^5.3.0",
    "ora": "^7.0.0"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "@types/node": "^20.8.0"
  }
}
