{
  "name": "@cytrac/core",
  "version": "0.1.0",
  "description": "Core analysis engine for TypeScript, JavaScript, and Python",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "jest",
    "lint": "eslint src/**/*.ts",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "ts-morph": "^20.0.0",
    "eslint": "^8.50.0",
    "glob": "^10.3.0"
  },
  "devDependencies": {
    "typescript": "^5.2.2"
  },
  "peerDependencies": {
    "@types/node": "^20.0.0"
  }
}
